DISCORD_TOKEN=
GUILD_ID=
DATABASE_URL=
WEBSITE_BASE_URL=https://www.5thcavgaming.com
CLERK_SYNC_KEY=
BATTALION_TIMEZONE=America/New_York
VOICE_FLUSH_SECONDS=300

# AUTO-JOIN REQUIREMENTS
# DISCORD_TOKEN must belong to the SAME Discord Application as the website DISCORD_CLIENT_ID.
# Battalion Clerk must already be in GUILD_ID, have MANAGE_ROLES, and its role must sit above every role it manages.
# Enable the Discord Developer Portal Server Members Intent because onboarding/role reconciliation reads guild members.
# The Website requests identify + guilds.join from applicants and holds the user grant securely until approval.

# HELL LET LOOSE: VIETNAM RCON — keep HLL_RCON_PASSWORD in Railway Variables only.
HLL_RCON_ENABLED=false
HLL_RCON_HOST=64.31.40.206
HLL_RCON_PORT=7779
HLL_RCON_PASSWORD=
HLL_RCON_POLL_SECONDS=5
HLL_RCON_CM_PER_METER=100
HLL_RCON_MAX_SPEED_MPS=130
HLL_RCON_RECONNECT_SECONDS=15
HLL_SEED_STOP_PLAYERS=50

# HLL VIP / reserved-slot synchronization
HLL_VIP_SYNC_ENABLED=true
HLL_VIP_RESERVED_SLOTS=2

# Website member status check automation
WEBSITE_STATUS_CHECK_DAYS=14
WEBSITE_NEVER_LOGIN_CHECK_DAYS=3
WEBSITE_STATUS_CHECK_REPEAT_DAYS=30

# Game identity reminder cadence for established Members without a verified/pending link.
GAME_LINK_REMINDER_DAYS=3

# Temporary Discord marker for newly joined community members
NEW_ARRIVAL_ROLE_NAME=NEW ARRIVAL
NEW_ARRIVAL_DAYS=7
# Canonical public publication routing (stable IDs are the current 1/5 Cav channels).
# Stored slash-command routes still take priority over these fallbacks.
HONORS_PROMOTIONS_CHANNEL_ID=1537242629131993159
HONORS_PROMOTIONS_CHANNEL_NAME=honors-and-promotions
HEADQUARTERS_ORDERS_CHANNEL_ID=1534357136858157157
HEADQUARTERS_ORDERS_CHANNEL_NAME=orders-from-headquarters


# Server Watch — suspicious-player review alerts (both official HLL servers)
SERVER_ADMIN_ROLE_NAME=Server Admin
SERVER_WATCH_ENABLED=true
SERVER_WATCH_INTERVAL_SECONDS=30
SERVER_WATCH_MIN_CONNECTED_SECONDS=600
SERVER_WATCH_MIN_KILLS=25
SERVER_WATCH_KPM=1.75
SERVER_WATCH_HIGH_KD=8.0
SERVER_WATCH_ZERO_DEATH_KILLS=30
SERVER_WATCH_BURST_2M=10
SERVER_WATCH_BURST_5M=18
SERVER_WATCH_REPEAT_MINUTES=15

# V91 Server Watch Intelligence — short-lived public-player movement evidence
SERVER_WATCH_POSITION_HISTORY_MINUTES=10
SERVER_WATCH_DIRECT_ROUTE_RATIO=0.88
SERVER_WATCH_DIRECT_ROUTE_MIN_METERS=400

# V92 shared Server Watch case channel. Set ID for strongest routing; name is fallback.
SERVER_WATCH_CHANNEL_ID=0
SERVER_WATCH_CHANNEL_NAME=server-watch

# Discord-only prospects: remind every N days until a Recruiting Case exists.
APPLICATION_REMINDER_DAYS=3
